import java.util.Random;
import java.util.Scanner;


public class RandomNumberGame {
    
    public int number; //this holds the computer's randomly chosen number 
            
    public int guess; //this holds what we're guessing
    
    public int maxValue; //this holds the max value
    
    public static Scanner scan; //this is its own class that is found within the standard Java library
    //must tell Java that we're using them in our program
    
    public static Random rand; //this is its own class that is found within the standard Java library
    //must tell Java that we're using them in our program
    
    public static void main (String [] args) {
        scan = new Scanner(System.in); //initialize classes scanner and random number generator
        we want scan to be a new Scanner, what are we gonna scan? whatever we putinto the scnnathe guess. 
        rand - new Random();
        number = rand.nextInt(); //number is going to be equal to the next random 
        System.out.println("Please enter a guess: ");
